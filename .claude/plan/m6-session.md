# M6: Interactive Session — Full Feature Set

Complete remaining interactive session features from Requirements.md.

## M6.1: Move vs Copy Selection

Toggle default action (copy ↔ move) with keybind. `--move` CLI flag.
Status bar indicator.

## M6.2: Per-File Tag Editing

Show current tags. Add/remove interactively. Validate in real-time.
Apply to selection if in selection mode.

## M6.3: Deletion Preview

Separate screen listing files marked for deletion. Scroll, confirm
(`y`), or cancel (`n`/`Esc`). Option to unmark from preview.

## Exit Criteria

All mandatory interactive session features from Requirements.md
implemented and tested.

## Tasks

- [ ] Status bar indicator for default action
- [ ] Keybind `M` to toggle copy ↔ move
- [ ] `--move` CLI flag
- [ ] Tag display in file detail area
- [ ] Apply tag to selection
- [ ] Deletion preview screen with scroll
- [ ] Confirm / cancel / unmark from preview
- [ ] Test: toggle default action
- [ ] Test: per-file tag editing
- [ ] Test: deletion preview + confirmation
- [ ] Test: deletion preview + cancellation
